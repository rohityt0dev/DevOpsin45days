# Using Docker as Jenkins Agent

👉 Run a Docker container as Jenkins agent

So each build runs inside temporary container environment.

### Prerequisites
Install Docker on the host, Java on Jenkins, and required plugins like Docker Pipeline or Docker plugin.

Jenkins user added to the docker group (on Linux)
