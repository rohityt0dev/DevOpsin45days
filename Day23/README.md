## Conatainers :
Containers are a way to package an application with all its dependencies (code, libraries, configs) so it can run anywhere without problems.

What is a Virtual Server ?

A virtual server is a software-based server that runs inside a physical machine using virtualization technology.
