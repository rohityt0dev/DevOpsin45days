Multi Stage Docker 
A multi-stage build means using multiple FROM instructions in one Dockerfile to create smaller, secure, production-ready images.
  
Multi-stage Docker build helps create optimized production images by separating build and runtime environments, reducing image size and improving security.
